<h3>Categories:</h3>
<% @categories.each do |category| %>
  | <%= link_to category.title, category_path(category) %> |
<% end %>
 <br> <br>

<h3>Items for Sale:</h3>
<% @items.each do |item| %>

<%= render partial: 'store/list_items', locals: {item: item} %>

<% end %>